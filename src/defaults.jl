
const DEFAULT_CONNECT_ARGS = (
    verbose= true,
    pedantic = true,
    tls_required = false,
    auth_token = nothing,
    user = nothing,
    pass = nothing,
    name = nothing,
    lang = NATS_CLIENT_LANG,
    version = NATS_CLIENT_VERSION,
    protocol = nothing,
    echo = nothing,
    sig = nothing,
    jwt = nothing,
    no_responders = nothing,
    headers = nothing,
    nkey = nothing
)
